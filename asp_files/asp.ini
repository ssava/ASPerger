[server]
; ASPerger server configuration (overridden by launch.json / CLI args)
; Uncomment and change values as needed:

; host = 127.0.0.1
; port = 9090
; default_document = index.asp
; enable_directory_listing = false
